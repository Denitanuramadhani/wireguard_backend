# ======================
# LDAP CONFIGURATION
# ======================
LDAP_SERVER=ldap://117.53.44.59:389
LDAP_BASE_DN=dc=example,dc=com
LDAP_USER_DN=uid={},ou=people,dc=example,dc=com
LDAP_ADMIN_DN=cn=admin,dc=example,dc=com
LDAP_ADMIN_PASSWORD=_l3Rum*Foca

# ======================
# MYSQL CONFIGURATION
# MySQL server berada di VPS terpisah: 117.53.45.105 (AlmaLinux)
# Hanya menyimpan: device, key, IP, status VPN
# User identity & role tetap di LDAP
# 
# Untuk Docker Compose: gunakan service name "mysql" sebagai MYSQL_HOST
# Untuk remote server: gunakan IP atau hostname
# ======================
MYSQL_HOST=mysql
# MYSQL_HOST=117.53.45.105  # Uncomment untuk remote MySQL
MYSQL_PORT=3306
MYSQL_USER=wgadmin
MYSQL_PASSWORD=your_mysql_password_here
MYSQL_DATABASE=wireguard_vpn
MYSQL_POOL_SIZE=10
MYSQL_MAX_OVERFLOW=20
MYSQL_CONNECT_TIMEOUT=10
MYSQL_ROOT_PASSWORD=rootpassword  # Untuk Docker MySQL container

# ======================
# WIREGUARD CONFIGURATION
# ======================
WG_INTERFACE=wg0
WG_CONF_PATH=/etc/wireguard/wg0.conf
WG_SERVER_PUBLIC_KEY=w+JEv1CDgtrOduepTLLW90rekFgHwvKoJTBsPEBOXDo=
WG_ENDPOINT=117.53.44.59:51820

# ======================
# JWT CONFIGURATION
# ======================
JWT_SECRET=your_jwt_secret_key_here_change_this_in_production
JWT_ALGO=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRE_DAYS=1

# ======================
# REDIS CONFIGURATION (untuk rate limiting & caching)
# 
# Untuk Docker Compose: gunakan service name "redis" sebagai REDIS_HOST
# Untuk remote server: gunakan IP atau hostname
# ======================
REDIS_HOST=redis
# REDIS_HOST=127.0.0.1  # Uncomment untuk local Redis
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=  # Optional: set password jika Redis menggunakan AUTH

# ======================
# APPLICATION CONFIGURATION
# ======================
ENVIRONMENT=development
DEBUG=True
LOG_LEVEL=INFO

# ======================
# SECURITY CONFIGURATION
# ======================
CORS_ORIGINS=http://localhost:3000,http://localhost:8080
MAX_DEVICES_PER_USER=3
QR_CODE_EXPIRATION_MINUTES=30

# ======================
# ENCRYPTION CONFIGURATION (untuk private key encryption)
# ======================
# Generate 32-byte key: python -c "import secrets; print(secrets.token_hex(32))"
ENCRYPTION_KEY=your_32_byte_encryption_key_here

# ======================
# DEVICE EXPIRATION CONFIGURATION
# ======================
# Auto-revoke devices yang tidak digunakan lebih dari N hari
DEVICE_EXPIRATION_DAYS=90
